{{- if .Values.secrets.create -}}
# DEV-ONLY placeholder secret. All values are obvious CHANGEME fakes.
# Production must set secrets.create=false and manage
# {{ .Values.global.existingSecret }} via an external secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.global.existingSecret }}
  labels:
    {{- include "carbon-cloud.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: CHANGEME-fake-local-only
  clerk-issuer: CHANGEME
  node-join-token-secret: CHANGEME-fake-local-only
  join-token: CHANGEME-fake-local-only
{{- end -}}
