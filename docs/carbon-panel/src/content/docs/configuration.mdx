---
title: Configuration
description: CARBONPANEL settings, config files, and environment variables.
---

import { Code } from '@astrojs/starlight/components';
import configExample from '../../../../../config.example.yaml?raw';

CARBONPANEL can be configured via a YAML config file, environment variables, or both. Environment variables take precedence over the config file.

For Minecraft server-specific configuration (server.properties, JVM flags, mod loader settings, etc.), see the [itzg/minecraft-server docs](https://docker-minecraft-server.readthedocs.io/).

## Config file

CARBONPANEL looks for `config.yaml` in these locations (in order):

1. Path passed via `-config` flag
2. Current working directory (`./`)
3. `./config/`
4. `/etc/CARBONPANEL/`

If no config file is found, defaults are used.

## Environment variables

Every config option can be set via an environment variable with the `CARBONPANEL_` prefix. Nested keys are separated by underscores:

```yaml
server:
  port: "8080"
```

is equivalent to:

```bash
CARBONPANEL_SERVER_PORT="8080"
```

## All options

<Code code={configExample} lang="yaml" title="config.example.yaml" />
