// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: cloud/v1/agent.proto

package cloudv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/athNdev/carbon-panel/pkg/proto/cloud/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AgentServiceName is the fully-qualified name of the AgentService service.
	AgentServiceName = "cloud.v1.AgentService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AgentServiceJoinNodeProcedure is the fully-qualified name of the AgentService's JoinNode RPC.
	AgentServiceJoinNodeProcedure = "/cloud.v1.AgentService/JoinNode"
	// AgentServiceConnectProcedure is the fully-qualified name of the AgentService's Connect RPC.
	AgentServiceConnectProcedure = "/cloud.v1.AgentService/Connect"
	// AgentServiceRenewCredentialsProcedure is the fully-qualified name of the AgentService's
	// RenewCredentials RPC.
	AgentServiceRenewCredentialsProcedure = "/cloud.v1.AgentService/RenewCredentials"
)

// AgentServiceClient is a client for the cloud.v1.AgentService service.
type AgentServiceClient interface {
	// JoinNode redeems a join token and returns node credentials.
	JoinNode(context.Context, *connect.Request[v1.JoinNodeRequest]) (*connect.Response[v1.JoinNodeResponse], error)
	// Connect opens the long-lived bidirectional control stream. The agent sends
	// AgentMessage frames and receives ControlMessage frames.
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Connect(context.Context) *connect.BidiStreamForClient[v1.AgentMessage, v1.ControlMessage]
	// RenewCredentials rotates the node's certificate before expiry.
	RenewCredentials(context.Context, *connect.Request[v1.RenewCredentialsRequest]) (*connect.Response[v1.RenewCredentialsResponse], error)
}

// NewAgentServiceClient constructs a client for the cloud.v1.AgentService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAgentServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AgentServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	agentServiceMethods := v1.File_cloud_v1_agent_proto.Services().ByName("AgentService").Methods()
	return &agentServiceClient{
		joinNode: connect.NewClient[v1.JoinNodeRequest, v1.JoinNodeResponse](
			httpClient,
			baseURL+AgentServiceJoinNodeProcedure,
			connect.WithSchema(agentServiceMethods.ByName("JoinNode")),
			connect.WithClientOptions(opts...),
		),
		connect: connect.NewClient[v1.AgentMessage, v1.ControlMessage](
			httpClient,
			baseURL+AgentServiceConnectProcedure,
			connect.WithSchema(agentServiceMethods.ByName("Connect")),
			connect.WithClientOptions(opts...),
		),
		renewCredentials: connect.NewClient[v1.RenewCredentialsRequest, v1.RenewCredentialsResponse](
			httpClient,
			baseURL+AgentServiceRenewCredentialsProcedure,
			connect.WithSchema(agentServiceMethods.ByName("RenewCredentials")),
			connect.WithClientOptions(opts...),
		),
	}
}

// agentServiceClient implements AgentServiceClient.
type agentServiceClient struct {
	joinNode         *connect.Client[v1.JoinNodeRequest, v1.JoinNodeResponse]
	connect          *connect.Client[v1.AgentMessage, v1.ControlMessage]
	renewCredentials *connect.Client[v1.RenewCredentialsRequest, v1.RenewCredentialsResponse]
}

// JoinNode calls cloud.v1.AgentService.JoinNode.
func (c *agentServiceClient) JoinNode(ctx context.Context, req *connect.Request[v1.JoinNodeRequest]) (*connect.Response[v1.JoinNodeResponse], error) {
	return c.joinNode.CallUnary(ctx, req)
}

// Connect calls cloud.v1.AgentService.Connect.
func (c *agentServiceClient) Connect(ctx context.Context) *connect.BidiStreamForClient[v1.AgentMessage, v1.ControlMessage] {
	return c.connect.CallBidiStream(ctx)
}

// RenewCredentials calls cloud.v1.AgentService.RenewCredentials.
func (c *agentServiceClient) RenewCredentials(ctx context.Context, req *connect.Request[v1.RenewCredentialsRequest]) (*connect.Response[v1.RenewCredentialsResponse], error) {
	return c.renewCredentials.CallUnary(ctx, req)
}

// AgentServiceHandler is an implementation of the cloud.v1.AgentService service.
type AgentServiceHandler interface {
	// JoinNode redeems a join token and returns node credentials.
	JoinNode(context.Context, *connect.Request[v1.JoinNodeRequest]) (*connect.Response[v1.JoinNodeResponse], error)
	// Connect opens the long-lived bidirectional control stream. The agent sends
	// AgentMessage frames and receives ControlMessage frames.
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Connect(context.Context, *connect.BidiStream[v1.AgentMessage, v1.ControlMessage]) error
	// RenewCredentials rotates the node's certificate before expiry.
	RenewCredentials(context.Context, *connect.Request[v1.RenewCredentialsRequest]) (*connect.Response[v1.RenewCredentialsResponse], error)
}

// NewAgentServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAgentServiceHandler(svc AgentServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	agentServiceMethods := v1.File_cloud_v1_agent_proto.Services().ByName("AgentService").Methods()
	agentServiceJoinNodeHandler := connect.NewUnaryHandler(
		AgentServiceJoinNodeProcedure,
		svc.JoinNode,
		connect.WithSchema(agentServiceMethods.ByName("JoinNode")),
		connect.WithHandlerOptions(opts...),
	)
	agentServiceConnectHandler := connect.NewBidiStreamHandler(
		AgentServiceConnectProcedure,
		svc.Connect,
		connect.WithSchema(agentServiceMethods.ByName("Connect")),
		connect.WithHandlerOptions(opts...),
	)
	agentServiceRenewCredentialsHandler := connect.NewUnaryHandler(
		AgentServiceRenewCredentialsProcedure,
		svc.RenewCredentials,
		connect.WithSchema(agentServiceMethods.ByName("RenewCredentials")),
		connect.WithHandlerOptions(opts...),
	)
	return "/cloud.v1.AgentService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AgentServiceJoinNodeProcedure:
			agentServiceJoinNodeHandler.ServeHTTP(w, r)
		case AgentServiceConnectProcedure:
			agentServiceConnectHandler.ServeHTTP(w, r)
		case AgentServiceRenewCredentialsProcedure:
			agentServiceRenewCredentialsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAgentServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAgentServiceHandler struct{}

func (UnimplementedAgentServiceHandler) JoinNode(context.Context, *connect.Request[v1.JoinNodeRequest]) (*connect.Response[v1.JoinNodeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("cloud.v1.AgentService.JoinNode is not implemented"))
}

func (UnimplementedAgentServiceHandler) Connect(context.Context, *connect.BidiStream[v1.AgentMessage, v1.ControlMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("cloud.v1.AgentService.Connect is not implemented"))
}

func (UnimplementedAgentServiceHandler) RenewCredentials(context.Context, *connect.Request[v1.RenewCredentialsRequest]) (*connect.Response[v1.RenewCredentialsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("cloud.v1.AgentService.RenewCredentials is not implemented"))
}
